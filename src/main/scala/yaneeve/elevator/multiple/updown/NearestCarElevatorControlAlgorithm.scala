package yaneeve.elevator.multiple.updown
import yaneeve.elevator.Floor

class NearestCarElevatorControlAlgorithm extends ElevatorControlAlgorithm {
  override def receiveFloorRequest(elevators: Elevators, floor: Floor): Elevators = ???

  override def step(elevator: Elevators): Elevators = ???
}
